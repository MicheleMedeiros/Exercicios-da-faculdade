#include <stdio.h>
#include <string.h>

//mostra quem vem primeiro de acordo com a ordem alfabetica
int main(int args, char** argv){
	
	char *a, *b;
	a = "abacate";
	b = "aba";
	
	if (*a < *b){
		printf("%s vem antes de %s\n", a, b);
	}else{
		printf("%s vem depois de %s\n", a, b);
	}
}
